import 'package:flutter/material.dart';

//キャンパスツアーの順番表示するページの親（仮）
class CourseMaker extends StatelessWidget {
  const CourseMaker({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    );
  }
}